<form method="post" action="{{ path('ressources_delete', {'id': ressource.id}) }}" onsubmit="return confirm('Pauvre fou.olle ! Es-tu sûr.e de vouloir supprimer cette merveille ressource {{ ressource.name }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource.id) }}">
    <button class="save-btn">Suppr.</button>
</form>
