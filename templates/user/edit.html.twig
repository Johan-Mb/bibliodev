{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

<div class="contenant">
        <div class="contenu">

			{% include "headpage.html.twig" %}

            <div class="big-buttons">
                    <div></div>
                    <div class="admin-btn-static">Profil de {{ user.username }}</div>
                    <div></div>
            </div>
    {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('themes_index') }}"><i class="far fa-arrow-alt-circle-left"></i></a>

        </div>
</div>
{% endblock %}
