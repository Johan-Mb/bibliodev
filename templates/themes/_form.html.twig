{{ form_start(form) }}
    <div class="row">

        <div class="col">
            {{ form_label(form.name, 'Nom du thème', {'label_attr': {'class': 'label-form'}}) }}
            {{ form_row(form.name, {'attr': {'class': 'input-form'}}) }}
        </div>
        <div class="col">
            {{ form_label(form.description, 'Description du thème (facultatif)', {'label_attr': {'class': 'label-form'}}) }}
            {{ form_row(form.description, {'attr': {'class': 'input-form'}}) }}
        </div>
        <button class="save-btn">{{ button_label|default('Ajouter à ma Bibliodev') }}</button>
{{ form_end(form) }}
