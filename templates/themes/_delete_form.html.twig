<form method="post" action="{{ path('themes_delete', {'id': theme.id}) }}" onsubmit="return confirm('Hey gourgandin.e! Es-tu sûre de vouloir supprimer le thème {{ theme.name }} ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme.id) }}">
    <button class="save-btn">Suppr.</button>
</form>
